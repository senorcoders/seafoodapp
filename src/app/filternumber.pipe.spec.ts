import { FilternumberPipe } from './filternumber.pipe';

describe('FilternumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FilternumberPipe();
    expect(pipe).toBeTruthy();
  });
});
